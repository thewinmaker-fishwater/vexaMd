/**
 * Markdown Editor - stub module
 * Editor functionality is now handled directly in main.js orchestrator.
 * This module is kept for backward compatibility with the import.
 */

class MarkdownEditor {
  init() {
    // No-op: editor initialization is handled in main.js
  }
}

export const markdownEditor = new MarkdownEditor();
